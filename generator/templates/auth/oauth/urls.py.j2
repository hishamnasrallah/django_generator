"""
OAuth URLs for {{ project.name }}

Generated by Django Enhanced Generator on {{ now().strftime('%Y-%m-%d %H:%M:%S') }}.
"""
from django.urls import path, include
from .views import (
    social_connections,
    disconnect_social,
    api_social_connections,
    api_disconnect_social,
{% if features.authentication.social_login_popup %}
    social_login_popup,
{% endif %}
)

app_name = 'oauth'

urlpatterns = [
    # Social account management
    path('connections/', social_connections, name='social_connections'),
    path('disconnect/<str:provider>/', disconnect_social, name='disconnect_social'),

    # API endpoints
    path('api/connections/', api_social_connections, name='api_social_connections'),
    path('api/disconnect/<str:provider>/', api_disconnect_social, name='api_disconnect_social'),

    {% if features.authentication.social_login_popup %}
    # Popup login
    path('popup/complete/', social_login_popup, name='social_popup_complete'),
    {% endif %}

    # Include allauth URLs
    path('', include('allauth.urls')),
]