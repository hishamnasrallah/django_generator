"""
GraphQL schema for {{ app_name }} app.

Generated by Django Enhanced Generator on {{ now().strftime('%Y-%m-%d %H:%M:%S') }}.
"""
import graphene

from .queries import {{ app_name|title }}Query
from .mutations import Mutation
{% if has_subscriptions %}from .subscriptions import Subscription
{% endif %}


# Create schema
schema = graphene.Schema(
    query={{ app_name|title }}Query,
    mutation=Mutation,
    {% if has_subscriptions %}subscription=Subscription,
    {% endif %}
)